const assert = require('assert');
const request = require('supertest');
const app = require('../../app');

describe('Sample API Test', () => {

    // it('Running Sample test', (done) => {
    //     request(app)
    //         .get('/v1/product/1')
    //         .end((err, response) => {
    //             let r1 = { id: 1, name: 'CD', description: 'Very good CD', price: 1 };
    //             // assert( JSON.stringify( response.body ) === JSON.stringify(r1) );
    //             // console.log(  response.body );
    //             done();
    //         });
    // });

});